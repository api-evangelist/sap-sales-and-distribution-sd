naftiko: 1.0.0-alpha2
info:
  label: SAP Sales and Distribution (SD) SAP Sales Scheduling Agreement API — Schedule Line
  description: 'SAP Sales and Distribution (SD) SAP Sales Scheduling Agreement API — Schedule Line. 1 operations. Lead operation:
    Retrieve delivery schedule lines. Self-contained Naftiko capability covering one Sap Sales And Distribution Sd business
    surface.'
  tags:
  - Sap Sales And Distribution Sd
  - Schedule Line
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SAP_SALES_AND_DISTRIBUTION_SD_API_KEY: SAP_SALES_AND_DISTRIBUTION_SD_API_KEY
capability:
  consumes:
  - type: http
    namespace: sap-sd-sales-scheduling-agreement-schedule-line
    baseUri: https://sandbox.api.sap.com/s4hanacloud/sap/opu/odata/sap/API_SALES_SCHEDULING_AGREEMENT
    description: SAP Sales and Distribution (SD) SAP Sales Scheduling Agreement API — Schedule Line business capability. Self-contained,
      no shared references.
    resources:
    - name: A_SchAgrmtScheduleLine
      path: /A_SchAgrmtScheduleLine
      operations:
      - name: listschedulelines
        method: GET
        description: Retrieve delivery schedule lines
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
    authentication:
      type: bearer
      token: '{{env.SAP_SALES_AND_DISTRIBUTION_SD_API_KEY}}'
  exposes:
  - type: rest
    namespace: sap-sd-sales-scheduling-agreement-schedule-line-rest
    port: 8080
    description: REST adapter for SAP Sales and Distribution (SD) SAP Sales Scheduling Agreement API — Schedule Line. One
      Spectral-compliant resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/a-schagrmtscheduleline
      name: a-schagrmtscheduleline
      description: REST surface for A_SchAgrmtScheduleLine.
      operations:
      - method: GET
        name: listschedulelines
        description: Retrieve delivery schedule lines
        call: sap-sd-sales-scheduling-agreement-schedule-line.listschedulelines
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: sap-sd-sales-scheduling-agreement-schedule-line-mcp
    port: 9090
    transport: http
    description: MCP adapter for SAP Sales and Distribution (SD) SAP Sales Scheduling Agreement API — Schedule Line. One tool
      per consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: retrieve-delivery-schedule-lines
      description: Retrieve delivery schedule lines
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: sap-sd-sales-scheduling-agreement-schedule-line.listschedulelines
      outputParameters:
      - type: object
        mapping: $.
